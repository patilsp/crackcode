---
title: CSS  
description: Master CSS fundamentals for web development interviews. Learn what CSS is, why it matters, and key topics to study.  
---  

# 🎨 CSS: Styling the Web  

**CSS (Cascading Style Sheets)** is the language that styles HTML content, controlling layout, colors, fonts, and responsiveness.  

## ✅ Why CSS Matters in Interviews  
- **Core Frontend Skill**: Every website uses CSS.  
- **Layout Challenges**: Interviewers test Flexbox/Grid knowledge.  
- **Responsive Design**: Mobile-friendly UIs are mandatory.  

---  

## 📚 Key CSS Topics to Study  

### 1. **Selectors & Specificity**  
- How to target HTML elements (classes, IDs, pseudo-classes).  
- Specificity hierarchy (`!important`, inline styles, IDs > classes > elements).  

### 2. **Box Model**  
- `width`, `height`, `padding`, `border`, `margin`.  
- `box-sizing: border-box` vs `content-box`.  

### 3. **Layout Systems**  
- **Flexbox**: 1D layouts (align items, justify content).  
- **CSS Grid**: 2D layouts (rows, columns, gaps).  
- **Positioning**: `static`, `relative`, `absolute`, `fixed`, `sticky`.  

### 4. **Responsive Design**  
- Media queries (`@media`).  
- Relative units (`rem`, `vw`, `%`).  
- Mobile-first vs desktop-first approaches.  

### 5. **Transitions & Animations**  
- `transition` for simple effects.  
- `@keyframes` for complex animations.  

### 6. **CSS Architecture**  
- BEM naming convention.  
- CSS-in-JS (Styled Components, Emotion).  

---  

## 🚀 Interview Tips  
- **Practice Layouts**: Recreate designs using Flexbox/Grid.  
- **Debugging**: Learn Chrome DevTools for CSS inspection.  
- **Performance**: Minimize repaints/reflows.  
